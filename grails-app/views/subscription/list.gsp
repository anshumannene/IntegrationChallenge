<div>
  <g:each var="user" in="${users}">
    ${user}
  </g:each>
</div>
<div>
  <g:each var="company" in="${companies}">
    ${company}
  </g:each>
</div>
<div>
  <g:each var="subscription" in="${subscriptions}">
    ${subscription}
  </g:each>
</div>